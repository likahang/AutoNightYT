# 專案長期規則

## 專案定位

本專案是 Windows 上的晚報 YouTube 縮圖批次生成器：讀取新聞文字檔，解析版型與文字指示，產生 Photoshop JSX，並由 Photoshop 套用 PSD 模板後輸出 PSD/JPG。

## 技術架構

- Python 3：文字解析、GUI、工作執行緒、JSX 產生器。
- GUI：PyQt5，入口為 `gui_main.py`；背景工作流程在 `worker.py`。
- Photoshop：必須使用 JSX 與既有 PSD 模板，不以其他繪圖引擎取代 Photoshop。
- 文字解析：`parse_thumbnail_txt.py`；JSX 產生：`generate_photoshop_script.py`。
- CLI 流程：`run_auto_thumbnail.py`。
- 模板：`晚報YT縮圖.psd`（大標版）及 `晚報YT縮圖(標圖版).psd`（標圖版）。
- CSV 設定檔使用既有 Big5/CP950 讀取方式；文字檔優先 UTF-8，失敗再嘗試 CP950。

## 必須遵守的限制

- 未經使用者明確要求，不要更換 PSD 模板、圖層名稱、Photoshop JSX 流程或輸出格式。
- 不要用 Windows GUI 操作取代可重現的本地測試；Photoshop 實機測試需使用者明確要求。
- 不要把 PSD、網路共享檔案、聊天紀錄、暫存 log 或測試輸出加入 Git。
- 修改前先保留既有未提交變更；不可使用 `git reset --hard` 或未經確認的刪除操作。
- 標圖版左邊字必須忽略引號變色；`(左邊字)`、`(左邊直字)` 及帶冒號格式都要維持相容。
- 左邊字的單一半形數字或標點（如 `3`、`!`）轉全形並留在主層；連續兩字元以上的半形數字／標點組合（如 `!!`、`3%`、`3:1`、`-3!`、`300`）另建橫向圖層。獨立層寬度需與主文字層最後一個非空白字相同，高度取兩個替代空格加該字串 Kerning 預留區的 95%，保留陰影安全距離，中心對齊主文字層且不移動主文字層；純兩位數 kerning=90，其他目前規則為 110。新版標圖版模板的左邊字初始為直的，完成定位後必須把主文字層及所有獨立橫向層一起旋轉 -4.08°。
- 標圖版 `直標` 群組最終高度不可超過 1000px；大標1只做水平變形至 W=1380px，大標2只做水平變形至 W=1340px，垂直比例固定 100%，不做等比縮放，也不再套用額外的最終寬度上限。
- 大標版大標1固定縮放至 W=1500px，最後一字基線位移為 -30px；此規則不可套用到標圖版。
- 大標1與大標2出現中文引號 `「」` 時，只有大標版將兩種符號字級設為 400pt，並將 `「` baseline shift 設為 80pt；標圖版不改 `「」` 字級，也不設定 baseline shift。若 `「` 位於行首、前方沒有文字，不設定 `「` 前方 Kerning，改為將整行大標固定向左移 140px；若對應的 `「` 前方有文字且 `」` 後方也有文字，則將 `「` 前方 Kerning 與 `」` 後方 Kerning 設為 -300，不修改 `」` 前方 Kerning。若去除尾端空白後最後一字是 `」`，以左側為固定點將大標寬度往右增加 70px。多個 `kerningRange` 必須依 `From` 由大到小寫入。
- 大標文字出現 `%` 時使用版型固定字級：大標版 350pt，標圖版 200pt。保留 `%` 原本的字型、顏色與其他樣式；不再使用百分比或可見 bounds 量測。同行多個 `%` 必須在同一個 `textStyleRange` ActionList 內一次寫入。
- 效果字 `字小` 與 `字可以小一點` 使用版型及行數固定字級：大標版大標1為 450pt、大標2為 441pt；標圖版大標1為 250pt、大標2為 245pt。其他效果字仍依 `效果字處理.csv`。
- 大標寬度水平變形必須在效果字、`%`、`「」` 字級與 Kerning 全部完成後執行，確保一般大標的最終寬度符合版型設定；行首 `「` 左移 140px 與行尾 `」` 向右增加 70px 的位置例外則在寬度變形後套用。
- 大標1與大標2的書名號 `《》` 不修改字級或 baseline shift。若 `《` 前方有文字，將 `《` 前方 Kerning 設為 2300；若 `《` 位於行首，不設定前方 Kerning，完成寬度變形後整行向左移 100px。只要 `》` 後方有文字，就將 `》` 後方 Kerning 設為 -200；不修改 `》` 前方 Kerning。
- 標圖版圖片必須以智慧型物件匯入；資料夾名稱需兼容 `晚報yt縮圖` 與 `YT縮圖`，並支援當日及月份歸檔路徑。
- 若行尾已知效果字（目前含漫畫爆炸、漫畫驚訝、卡通大火）只有右括號、漏打左括號，例如 `標題 綠色漫畫爆炸)`，解析前須正規化為 `標題 (綠色漫畫爆炸)`，將其從大標文字分離並記入效果字。

## Coding conventions

- Python 以清楚的小函數處理解析與 JSX 片段生成；維持既有函式命名與繁體中文註解風格。
- JSX 字串插值必須正確處理 Windows/UNC 路徑與 Unicode；不可直接拼接未轉義的使用者文字。
- 新增規則要有對應的單元測試或至少更新既有測試斷言。
- 使用 `apply_patch` 修改檔案；完成後執行語法檢查與測試。

## Build / test

```powershell
python -m unittest test_layout_variants.py
python -m py_compile parse_thumbnail_txt.py generate_photoshop_script.py gui_main.py worker.py
開啟本地測試.bat
```

正式打包使用 `build_exe.bat`（需在 Windows、已安裝 PyInstaller 的環境執行）。

## 固定的技術選擇

- Photoshop JSX + PSD 模板是既定產線，不改成 ImageMagick、PIL 或其他渲染方式。
- 標圖版圖片使用 Photoshop Embedded Smart Object，避免縮放後模糊。
- GUI 以 PyQt5 維持現有流程；網路來源與輸出仍依使用者選定日期切換。
