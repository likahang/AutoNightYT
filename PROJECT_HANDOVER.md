# 晚報 YouTube 縮圖生成器｜交接文件

更新日期：2026-08-20

## 1. 專案目前目標

維護一套 Windows/Photoshop 自動化工具，讓使用者選擇某日的晚報文字檔後，按下開始生成，即可同時處理「大標版」與「標圖版」縮圖，產生 PSD 及 JPG。

## 2. 現在正在處理的任務

目前主要在交接與收尾階段：整理長期規則並準備把專案交給下一個 Codex session。最近一項功能調整是標圖版雙圖拼接的右圖向左位移由 100px 改為 215px。

## 3. 已完成內容

- 支援版型判定：含圖片指示與左邊字的文字檔判為標圖版，其他為大標版。
- 左邊字標記支援 `(左邊字)`、`(左邊直字)`、`(左邊字:文字)`、`(左邊直字:文字)`。
- 找到左邊字卻找不到完整括號圖片指示時，產生驗證錯誤並略過該檔。
- 左邊字完全忽略引號變色，替換 `直標` 群組中的 `直標直標` 圖層。
- 數字處理：所有數字都是一位數時轉全形留在主層；含多位數或小數時，主層以兩個空格保留位置，數字另建橫向圖層。
- 多位數數字圖層 W=250；兩位整數 kerning=90，其他目前 kerning=110；數字圖層與主文字置中，不移動主文字層。
- `直標` 群組最終 H 上限 1000px；標圖版大標1 W 上限 1400px，大標2 W 上限 1280px。
- 圖片以智慧型物件匯入 `圖片` 群組，W=1400、中心 Y=260；單圖套用收縮 20px 遮色片與 Feather 20px。
- 支援 `+`/`＋` 雙圖指示；依文字順序配對左右圖。左圖套用右側漸層虛化，右圖置於左圖下方，並由左圖右側向左移 215px。
- 圖片配對支援當日與月份歸檔路徑，並兼容資料夾名稱 `晚報yt縮圖`、`YT縮圖`。
- GUI 選取其他日期資料夾時會切換日期，文字來源、PSD 輸出與圖片配對跟著日期走。
- `build_exe.bat` 已補入 `效果字處理.csv`，並加上 `--noupx`。

## 4. 尚未完成內容

- 尚未確認 NAS `10.227.63.105` 的圖片共享權限；目前從此環境測試兩條 0815 路徑都得到 Access denied。
- 尚未在實際 Photoshop 環境完整跑完 0815 全批次；本地 JSX/解析測試不等於 Photoshop 實機驗證。
- 已建立 WIP checkpoint commit `cff2718`（`chore: add handover checkpoint`）；尚未 push 到遠端。
- 雙圖漸層與數字定位仍需以使用者提供的實際 PSD/圖片做視覺驗證。

## 5. 最近修改過哪些檔案及原因

- `parse_thumbnail_txt.py`：新增標圖版解析、左邊字格式、圖片指示驗證、雙圖拆分、日期/月份圖片路徑與資料夾名稱相容。
- `generate_photoshop_script.py`：新增標圖版 JSX 流程、左邊字數字處理、Smart Object 圖片匯入、遮色片、雙圖拼接及尺寸上限；右圖位移改為 215px。
- `gui_main.py`：依瀏覽到的日期資料夾切換日期、預覽與批次生成整合標圖版/大標版。
- `worker.py`：背景批次流程使用選定日期解析檔案並產生兩種版型。
- `build_exe.bat`：補入必要 CSV 資源並停用 UPX。
- `test_layout_variants.py`：覆蓋版型判定、日期、左邊字、圖片配對與 JSX 生成斷言；雙圖位移斷言目前為 215px。
- `AGENTS.md`：本次新增，僅記錄長期有效的架構、限制、規範與測試方式。

## 6. 已嘗試但失敗的方法

- 直接從本環境存取 `\\10.227.63.105\public\__CG-IN\0815\1800\YT縮圖` 與 `...\08月\0815\1800\YT縮圖`：失敗，系統回報 Access denied；不能據此判斷資料夾不存在，需在有 NAS 權限的 Windows 使用者環境確認。
- 以批次檔直接啟動本地測試：曾因 Windows 批次檔編碼/中文命令顯示錯誤而閃退；目前應保留 UTF-8 相容處理並從命令列查看 log。
- 嘗試直接 `git add/commit`：目前 sandbox 對 `.git/index.lock` 無寫入權限；需要使用者授權的 Git 操作或在可寫 Git 環境完成。

## 7. 重要架構決策

- Photoshop JSX + PSD 模板是既定產線，不能用 PIL/ImageMagick 取代。
- 標圖版使用 `晚報YT縮圖(標圖版).psd`；大標版使用 `晚報YT縮圖.psd`。
- 圖片必須匯入為 Embedded Smart Object，避免重新縮放失真。
- 網路路徑依日期：文字來源通常是 `\\10.227.58.117\新聞txt\MMDD\1800`，PSD 輸出通常是 `\\10.227.58.117\新聞psd\MMDD\縮圖`；圖片根目錄目前設定為 `\\10.227.63.105\public\__CG-IN`，再尋找當日或 `MM月\MMDD` 歸檔。

## 8. 目前已知 bug / 風險

- NAS 圖片共享目前可能是權限、登入認證或實際 share 名稱問題；程式已支援資料夾名稱變體，但無法繞過 Windows/NAS 權限。
- GUI/worker 的網路路徑在不同電腦可能不可用，需保留可配置或至少清楚提示。
- JSX 的 Photoshop DOM/ActionDescriptor 行為需要實機確認；單元測試只檢查產生的文字，不會執行 Photoshop。
- 工作區含大量本地測試檔、log、chat.json 與測試輸出，不應全部加入 Git。

## 9. 測試結果與 Git 狀態

最近測試：

```text
python -m unittest test_layout_variants.py
Ran 14 tests ... OK
```

目前 `git status` 顯示：

- 已提交：核心程式、`AGENTS.md`、本文件及 `test_layout_variants.py` 均已納入 `cff2718`。
- 仍未追蹤：一個測試用文字檔、`chat.json`、`copilot-context.md`、`local_test_startup.log`、測試用 CSV、測試資料夾與本地測試 BAT；這些是本機素材／紀錄，不應加入 Git。
- 工作樹沒有已追蹤檔案的修改；`git diff` 與 `git diff --check` 均無差異（Git 全域 ignore 權限警告可忽略）。

文件更新前 `git diff --stat` 顯示核心 5 個程式檔約 1196 行變更；更新文件後應重新執行 `git status` 與 `git diff --stat`。

## 10. 下一步應該做什麼

1. 在有權限的 Windows/NAS 環境確認 0815 圖片實際共享路徑與登入權限。
2. 用 `測試用文字檔`、`測試用文字檔2` 做解析與圖片配對驗證，確認遺失圖片會略過並提示。
3. 在 Photoshop 實機測試一張單圖、一張雙圖、含一位數、兩位數及小數的標圖版。
4. 確認輸出 PSD/JPG 與圖層位置後，再決定是否要微調 JSX。
5. 使用者明確確認遠端與分支後，在具備 `.git` 寫入權限的環境提交並 push。

## 11. 下一個 agent 最需要知道的上下文

不要把 NAS Access denied 當成「找不到圖片」直接改路徑；先確認 Windows 帳號能否開啟 share。不要重寫 Photoshop 流程，也不要把未追蹤測試素材全部提交。使用者偏好先本地測試、避免控制 Windows GUI；任何主要功能修改前應先說明並保留現有規則。
