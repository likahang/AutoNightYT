# 自動縮圖生成系統 - 程式流程說明

## 📋 系統概述
這是一個自動化 Photoshop 縮圖生成系統，可以批量處理文字檔案，自動生成 PSD 和 JPG 縮圖。

---

## 🔄 完整工作流程

### **第 1 步：初始化設定**
```
執行 執行自動縮圖.bat
  ↓
啟動 Python 虛擬環境
  ↓
執行 run_auto_thumbnail.py
```

**工作內容：**
- 切換 Windows 編碼為 Big5（支援中文）
- 進入 Python 虛擬環境
- 啟動主程式

---

### **第 2 步：獲取日期**
```
系統自動取得今天日期（MMDD 格式）
範例：0129
```

**用途：**
- 作為輸出檔名前綴
- 確定網路路徑：`\\10.227.58.117\新聞psd\0129\縮圖`

---

### **第 3 步：獲取製作者名稱**
```
彈出 Tkinter 對話框
提示：「請輸入製作者名稱」
```

**工作內容：**
- 使用者輸入製作者名稱（例如：桁）
- 名稱會附加在 PSD 檔名後面

**範例：**
```
PSD 檔名：0129_1800 晚報YT縮圖1 高市爆哭_桁.psd
JPG 檔名：0129_1800 晚報YT縮圖1 高市爆哭.jpg
```

---

### **第 4 步：選擇文字檔案**
```
彈出檔案選擇對話框
預設路徑：\\10.227.58.117\新聞txt\0129\1800
允許多選：可同時選擇多個 .txt 檔案
```

**工作內容：**
- 使用者可選擇 1 個或多個文字檔案
- 按 OK 確認，按 Cancel 取消程式

---

### **第 5 步：批量解析文字檔案**

#### **5.1 逐個處理每個文字檔**
```
for 每個選中的 .txt 檔案:
    ↓
    [解析階段]
    呼叫 parse_thumbnail_txt.py
    ↓
    提取關鍵資訊：
    - slag（標題+縮圖編號）
    - anchor（主播名字）
    - title_line1（第一行大標）
    - title_line2（第二行大標）
```

#### **5.2 文本內容驗證** ✅ **新增功能**
```
檢查提取結果：
├─ 第一行大標是否為空？
├─ 第二行大標是否為空？
└─ 如果有空值 → 打印錯誤 → 跳過此檔案 → 繼續下一個
```

**驗證失敗時：**
```
❌ 錯誤: 標題內容不完整
   - 第一行大標: '' (空值)
   - 第二行大標: '錯誤內容'
   請檢查文字檔內容是否正確
```

---

### **第 6 步：JSX 腳本生成**（如果驗證通過）

#### **6.1 載入配置檔案**
```
載入顏色方案：晚報變色.csv (UTF-8)
├─ 提取 RGB 顏色值
├─ 基礎色、邊框色、陰影色、特殊色等

載入右上變色方案：右上變色.csv (Big5)
├─ A 組、B 組、C 組、D 組
└─ 根據顏色 ID 自動排除衝突配色
```

#### **6.2 生成 ExtendScript（JSX）程式碼**
```
建立 modify_thumbnail_MMDD_COLORID_HASH.jsx

JSX 腳本功能：
├─ 開啟 PSD 檔案
├─ 修改標題文字
├─ 修改主播圖層
├─ 應用顏色方案
├─ 儲存為 PSD
├─ 匯出為 JPG（SaveForWeb）
└─ 清理記憶體並關閉
```

**檔名構成：**
- `modify_thumbnail_0129_G01_7FB118.jsx`
  - `0129`：日期
  - `G01`：顏色 ID
  - `7FB118`：檔案內容雜湊（防止重複）

---

### **第 7 步：設定 Photoshop 環境**

#### **7.1 修改 Windows 登錄檔**
```
路徑：HKEY_CURRENT_USER\Software\Adobe\Photoshop\2024\...
設定：
  DisableWarnBeforeExecutingUserAdobeScripts = 1
  Allow JavaScript Files To Read And Write Files = 1
```

#### **7.2 配置 PSUserConfig.txt**
```
位置：%APPDATA%\Adobe\Adobe Photoshop 2024\...
內容：WarnRunningScripts 0
```

**作用：**
- 禁用 Photoshop 腳本執行警告
- 自動執行 JSX，不需人工確認

---

### **第 8 步：依序執行 JSX 腳本**（核心自動化）

#### **8.1 逐個執行（不是並行）**
```
for 每個 JSX 檔案:
    ↓
    啟動 Photoshop：
    Photoshop.exe -r "jsx_path"
    ↓
    等待 PSD 檔案生成...
```

#### **8.2 智能監控機制** ✅ **改進功能**

**監控時間：**
- 最多等待 **600 秒（10 分鐘）**
- 每 2 秒檢查一次

**監控內容：**
```
while 時間未超時:
    ├─ 檢查是否有 .error_log.txt（JSX 出錯標記）
    │   └─ 發現錯誤 → 記錄 → 跳過 → 強制關閉 Photoshop
    │
    ├─ 檢查是否生成了新 PSD 檔案
    │   └─ 發現新檔 → 記錄 → 繼續下一個
    │
    └─ 檢查是否生成了 JPG（表示完全成功）
        └─ JPG 生成 → 標記完成 → 下一個檔案
```

#### **8.3 三種結果處理**

**情況 1：成功生成**
```
✓ 檔案已生成: 0129_1800 晚報YT縮圖1 高市爆哭_桁.psd
✓ 檔案已生成: 0129_1800 晚報YT縮圖1 高市爆哭.jpg
  ↓
✓ 檔案 1 已完成，準備執行下一個...
```

**情況 2：JSX 出錯** ✅ **新增功能**
```
❌ Photoshop 執行失敗: Could not transform...
  ↓
⚠ 檔案 1 執行失敗，跳過此檔案繼續下一個
  ↓
強制關閉 Photoshop（taskkill /IM Photoshop.exe /F）
  ↓
等待 2 秒，執行下一個
```

**情況 3：超時未生成** ✅ **新增功能**
```
已監控 600 秒...
  ↓
⚠ 檔案 1 在規定時間內未能生成 PSD，跳過此檔案繼續下一個
  ↓
強制關閉 Photoshop（taskkill /IM Photoshop.exe /F）
  ↓
等待 2 秒，執行下一個
```

---

### **第 9 步：清理暫存檔案**

```
刪除所有生成的 JSX 檔案
├─ modify_thumbnail_0129_G01_*.jsx
├─ modify_thumbnail_0129_G03_*.jsx
└─ ...（所有臨時 JSX 檔案）

刪除錯誤標記檔
└─ .error_log.txt（如有）
```

---

### **第 10 步：顯示結果統計**

```
==================================================
📊 處理結果統計
==================================================
✓ 成功生成: 8 個檔案
  - 0129_1800 晚報YT縮圖1 高市爆哭_桁.psd
  - 0129_1800 晚報YT縮圖1 高市爆哭.jpg
  - ...

❌ 生成失敗: 1 個檔案
  - 1800 晚報YT縮圖26 測試錯誤.txt
    (標題為空)

⏭️  超時跳過: 1 個檔案
  - 1800 晚報YT縮圖28 複雜內容.txt
    (Photoshop 執行超過 10 分鐘)
==================================================
```

---

### **第 11 步：自動關閉**

```
所有檔案處理完成
  ↓
顯示「程式將自動關閉...」
  ↓
等待 2 秒
  ↓
sys.exit(0)
```

---

## 📊 資料流向圖

```
選擇 TXT 檔案 (多個)
        ↓
┌─────────────────────────┐
│  逐個處理每個 TXT       │
│  [For 迴圈]             │
└────────┬────────────────┘
         ↓
    ┌─────────────────┐
    │ 解析文字內容     │
    └────────┬────────┘
         ↓
    文本驗證 ✅
    (檢查標題是否為空)
    ↙         ↘
  ✓ 通過      ✗ 失敗
    ↓         ↓
生成 JSX   跳過 → 下一個
    ↓
┌──────────────────────┐
│ 執行 JSX in PS       │
│ [監控 PSD/JPG 生成]  │
└────────┬─────────────┘
         ↓
    ┌──────────────────┐
    │ 統計成功/失敗    │
    │ 顯示結果         │
    └────────┬─────────┘
         ↓
    自動關閉程式
```

---

## 🛡️ 錯誤處理機制

| 問題 | 原因 | 處理方式 |
|------|------|---------|
| **文本驗證失敗** | 標題為空或不完整 | 跳過，不生成 JSX |
| **JSX 生成失敗** | Python 腳本錯誤 | 跳過，記錄失敗 |
| **Photoshop 出錯** | PSD 檔案損壞或記憶體不足 | 寫入 `.error_log.txt`，Python 偵測到後跳過 |
| **超時未完成** | 腳本執行時間過長 | 等待 600 秒後強制關閉 PS，跳過檔案 |
| **彈出警告對話框** | ~~安全提示~~ | ✅ 已禁用（登錄檔 + PSUserConfig.txt） |
| **Photoshop 卡住** | 前一個 JSX 未完全關閉 | ✅ 強制 taskkill，清理進程 |

---

## 📁 輸出目錄結構

```
\\10.227.58.117\新聞psd\0129\縮圖\
├─ 0129_1800 晚報YT縮圖1 高市爆哭_桁.psd
├─ 0129_1800 晚報YT縮圖1 高市爆哭.jpg
├─ 0129_1800 晚報YT縮圖2 測試_桁.psd
├─ 0129_1800 晚報YT縮圖2 測試.jpg
└─ ...
```

**命名規則：**
- **PSD**：`MMDD_Slag_製作者.psd`（保留製作者標記）
- **JPG**：`MMDD_Slag.jpg`（不包含製作者標記）

---

## ⚡ 性能特性

| 項目 | 設定 |
|------|------|
| 每個檔案超時 | 600 秒（10 分鐘） |
| JSX 執行方式 | 順序執行（不並行） |
| 色彩方案 | 隨機選擇（如未指定） |
| 記憶體清理 | 執行前調用 `app.purge()` |
| Photoshop 版本 | 2024 |
| 解析度支援 | 自動偵測，包含超寬螢幕 |

---

## 🔧 依賴元件

```
執行自動縮圖.bat
  ↓
run_auto_thumbnail.py（主程式）
  ├─ generate_photoshop_script.py（JSX 生成）
  │   └─ parse_thumbnail_txt.py（文本解析）
  ├─ 晚報變色.csv（顏色配置）
  ├─ 右上變色.csv（右上變色配置）
  ├─ 晚報YT縮圖.psd（PSD 範本）
  └─ Photoshop 2024（自動化執行）
```

---

## 🎯 使用案例

**批量處理 10 個文件：**
```
1. 執行 執行自動縮圖.bat
2. 輸入製作者名稱：桁
3. 選擇 10 個 .txt 檔案
4. 系統自動：
   - 驗證 10 個文件
   - 生成 10 個 JSX
   - 設定 Photoshop
   - 逐個執行 JSX
   - 產生 20 個輸出檔案（PSD + JPG）
5. 自動關閉，顯示統計報告
```

**預期耗時：**
- 10 個檔案 × 30 秒/檔 = 5 分鐘（正常情況）
- 包含網路延遲和 Photoshop 初始化

---

## 📝 重要改進 ✅

### 已實現的功能：
1. ✅ 多檔案批處理
2. ✅ 順序執行（防止並行衝突）
3. ✅ 文本驗證（提前檢測問題）
4. ✅ 自動錯誤偵測（不彈出對話框）
5. ✅ 超時自動跳過
6. ✅ 強制進程清理
7. ✅ 詳細統計報告
8. ✅ JPG 自動匯出（SaveForWeb）
9. ✅ 記憶體管理（app.purge()）
10. ✅ 製作者名稱後綴管理

---

## 💡 使用建議

1. **首次執行**：選擇 1-2 個檔案測試
2. **大批量處理**：建議分批進行（每批 20 個以內）
3. **監控網路**：確保 `\\10.227.58.117` 網路路徑可用
4. **Photoshop 記憶體**：建議分配 4GB 以上
5. **PSD 檔案**：確保 `晚報YT縮圖.psd` 完整無損壞

---

**最後更新：2026/01/29**
