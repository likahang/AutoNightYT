# 晚報YT縮圖自動化處理腳本流程說明

## 一、資料提取階段（Python程式）

### 1.1 讀取文字檔
- **路徑**：`\\10.227.58.117\新聞txt\{MMDD}\1819`
  - `MMDD`：今天的日期（月月日日，例如：0105）
- **目標檔案**：檔名包含「1800 晚報YT縮圖」的 `.txt` 檔案

### 1.2 解析文字檔內容
從文字檔中提取以下資訊：

1. **Slag**：文字檔的第一行內容
   - 範例：`1800 晚報YT縮圖2 賴清德認:台灣要最壞打算`

2. **主播名字**：從以下6個主播中找出
   - 林嘉源、鄭亦真、張雅婷、洪淑芬、麥玉潔、何橞瑢

3. **大標文字**：檔案最底二行（已移除效果字）
   - **第一行大標**：倒數第二行，移除括號內容
   - **第二行大標**：最後一行，移除括號內容
   - 範例：
     - 第一行：`"賴清德"認了:`（原本是 `"賴清德"認了:(漫畫爆炸)`）
     - 第二行：`"台灣要做"最壞打算"`

4. **變色字**：所有 `""` 內的文字
   - 範例：`['賴清德', '台灣要做', '最壞打算']`

5. **效果字**：所有 `()` 內的文字，但排除：
   - 含有「大底黑色」的效果字
   - 含有「何橞瑢」的效果字
   - 含有「漫畫驚訝調暗暗」的效果字
   - 範例：`['漫畫爆炸']`

### 1.3 生成Photoshop腳本
- 將解析結果轉換為 JSX 腳本
- 檔案名稱：`modify_thumbnail_{MMDD}.jsx`

---

## 二、Photoshop腳本執行階段（JSX腳本）

### 2.1 初始化
- 設定 `#target photoshop`
- 定義輔助函數：
  - `hexToRgb()`：將 Hex 轉換為 RGB
  - `setShapeColor()`：設定形狀圖層顏色

### 2.2 打開PSD檔案
- 打開：`晚報YT縮圖.psd`
- 檢查檔案是否存在

### 2.3 步驟1：重命名檔案
- **新檔名格式**：`{MMDD}_{Slag}.psd`
- **範例**：`0105_1800 晚報YT縮圖2 賴清德認_台灣要最壞打算.psd`
- **注意**：自動清理檔名中的不允許字元（如冒號 `:` 會變成底線 `_`）

### 2.4 步驟2：修改大標文字
- **找到「標」圖層群組**
- **找到兩個大標圖層**（檔名包含「大標」）
- **替換文字**：
  - 第一行大標 → 替換**第二個**大標圖層
  - 第二行大標 → 替換**第一個**大標圖層
- **範例**：
  - `titleLayers[1].textItem.contents = "第一行大標"`
  - `titleLayers[0].textItem.contents = "第二行大標"`

### 2.5 步驟3：調整大標圖層寬度
- **使用變形方式改變圖層寬度**（從左側縮放）
- **第一行大標**（`titleLayers[1]`）：
  - 目標寬度：**1600px**
  - 使用 `resize()` 方法，錨點位置：`AnchorPosition.MIDDLELEFT`（左側中間）
- **第二行大標**（`titleLayers[0]`）：
  - 目標寬度：**1620px**
  - 使用 `resize()` 方法，錨點位置：`AnchorPosition.MIDDLELEFT`（左側中間）
- **計算方式**：
  ```javascript
  var currentWidth = bounds[2] - bounds[0];
  var scale = targetWidth / currentWidth;
  layer.resize(scale * 100, 100, AnchorPosition.MIDDLELEFT);
  ```

### 2.6 步驟4：為大標圖層添加效果
為兩個大標圖層添加以下圖層效果：

#### 4.1 Outside Stroke（外部描邊）
- **大小**：15px
- **樣式**：外部（`outsetFrame`）
- **顏色**：黑色（RGB: 0, 0, 0）

#### 4.2 Drop Shadow（陰影）
- **混合模式**：正常（Normal）
- **不透明度**：100%
- **角度**：120度
- **距離**：10px
- **展開（Spread）**：100%
- **尺寸（Size）**：18px
- **顏色**：黑色（RGB: 0, 0, 0）

**技術實現**：使用 `ActionDescriptor` 和 `ActionReference` 來設定圖層效果

### 2.7 步驟5：設定主播圖層
- **找到「主播」圖層群組**
- **主播名字對應表**：
  ```
  林嘉源 → 林嘉源
  鄭亦真 → 鄭亦真 （注意尾隨空格）
  張雅婷 → 張雅婷
  洪淑芬 → 洪淑芬
  麥玉潔 → 麥玉潔
  何橞瑢 → 何橞瑢
  ```
- **操作**：
  1. 找到對應的主播圖層，設為可見（`visible = true`）
  2. 刪除其他所有主播圖層（`remove()`）

### 2.8 步驟6：修改「精華版_顏色可變」群組

#### 6.1 第一個圖層（索引0）替換成主播名字
- **目標**：`colorGroup.layers[0]`
- **操作**：如果是文字圖層，替換文字內容
  ```javascript
  nameLayer.textItem.contents = "主播名字";
  ```

#### 6.2 第4個圖層（索引3）隨機變色
- **目標**：`colorGroup.layers[3]`
- **顏色清單**（12種顏色，Hex格式）：
  ```
  "f05910" (橙紅色)
  "fe4701" (鮮紅色)
  "dc1000" (深紅色)
  "a700fe" (紫色)
  "7800ff" (藍紫色)
  "342292" (深藍色)
  "0031e5" (藍色)
  "181aab" (深藍色)
  "1f2966" (深藍灰色)
  "00a322" (綠色)
  "008f5a" (青綠色)
  "007e0b" (深綠色)
  ```
- **操作流程**：
  1. 從顏色清單中隨機選取一個顏色
  2. 將 Hex 轉換為 RGB
  3. 選擇目標圖層
  4. 使用 `setShapeColor()` 函數修改顏色

### 2.9 步驟7：保存檔案
- **保存位置**：與原始PSD相同的目錄
- **檔名**：使用步驟1中設定的新檔名
- **保存選項**：
  - 嵌入顏色配置檔
  - 保留 Alpha 通道
  - 保留圖層

---

## 三、完整流程圖

```
開始
  ↓
讀取文字檔（網路路徑）
  ↓
解析資料（Slag、主播、大標、變色字、效果字）
  ↓
生成JSX腳本
  ↓
在Photoshop中執行腳本
  ↓
打開PSD檔案
  ↓
1. 重命名檔案（MMDD_Slag.psd）
  ↓
2. 修改大標文字（第一行→第二個圖層，第二行→第一個圖層）
  ↓
3. 調整大標圖層寬度（第一行1600px，第二行1620px，從左側縮放）
  ↓
4. 為大標圖層添加效果（Outside Stroke 15px + Drop Shadow）
  ↓
5. 設定主播圖層（顯示對應主播，刪除其他主播）
  ↓
6. 修改「精華版_顏色可變」群組
   ├─ 6.1 第一個圖層替換成主播名字
   └─ 6.2 第4個圖層隨機變色（12種顏色中隨機選）
  ↓
7. 保存檔案
  ↓
完成
```

---

## 四、重要注意事項

### 4.1 檔案命名規則
- 自動清理不允許的字元：`< > : " / \ | ? *`
- 冒號 `:` 會替換為底線 `_`
- 移除連續的底線
- 限制長度為200字元

### 4.2 圖層結構要求
- **「標」群組**：必須包含至少2個大標圖層
  - 大標圖層必須是文字圖層
  - 用於寬度調整和效果添加
- **「主播」群組**：必須包含對應的主播圖層
- **「精華版_顏色可變」群組**：
  - 第一個圖層（索引0）：必須是文字圖層（用於替換主播名字）
  - 第4個圖層（索引3）：必須是形狀圖層或純色填充層（用於變色，通常是 Rectangle 1）

### 4.3 錯誤處理
- 所有步驟都有 try-catch 錯誤處理
- 會顯示詳細的錯誤訊息
- 如果某個步驟失敗，會繼續執行後續步驟

---

## 五、使用方式

### 5.1 生成腳本
```bash
python generate_photoshop_script.py
```

### 5.2 在Photoshop中執行
1. 打開 Photoshop
2. 選擇 **檔案 > 腳本 > 瀏覽...**
3. 選擇生成的 `.jsx` 檔案（例如：`modify_thumbnail_0105.jsx`）
4. 腳本會自動執行所有修改
5. 完成後會顯示成功訊息

---

## 六、輸出結果

- **新PSD檔案**：`{MMDD}_{Slag}.psd`
- **位置**：與原始PSD相同的目錄
- **內容**：已根據文字檔內容完成所有修改

