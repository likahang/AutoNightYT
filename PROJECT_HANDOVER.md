# 晚報 YouTube 縮圖生成器｜交接文件

更新日期：2026-08-23

## 1. 專案目前目標

維護 Windows 上的晚報 YouTube 縮圖批次生成器。程式讀取指定日期的新聞文字檔，解析「大標版」或「標圖版」，產生 Photoshop JSX，再由既有 PSD 模板輸出 PSD/JPG。現階段目標是穩定既有產線、完成最新文字排版規則的 Photoshop 實機驗證，之後再正式打包。

## 2. 現在正在處理的任務

最近一輪開發已完成並準備部署 GitHub，集中在：

- 圖片指示、缺圖與多圖的容錯。
- 標圖版左邊字中數字／標點的獨立橫向圖層。
- 大標 `「」`、`《》`、`%`、效果字字級與最終寬度順序。
- GUI 的縮圖設定欄、重新生成、工作階段紀錄與進度統計。
- 配色資料改讀 Google Sheet 並保留快取及本地 CSV 後備。

最新規則已寫入程式與單元測試；Photoshop 視覺結果仍由使用者持續以本地預覽確認。

## 3. 這次已完成內容

### 3.1 文字解析與圖片配對

- 支援 `(左邊字)`、`(左邊直字)` 及帶冒號文字格式。
- `(超大字)` 不再被誤判為圖片指示。
- 含 `定圖` 的括號行會視為圖片指示；例如 `(定圖 用今天 妤史努比)` 會把空格分隔的 `用今天`、`妤史努比` 各自當候選名稱，只要找到其中一張就繼續，全部找不到才警告並略過該檔。
- 支援 `+`／`＋` 多圖指示。多圖中某一項缺圖時只警告並略過該項，已找到的圖片仍會匯入；全部都找不到才略過整份文字檔。
- 日期推斷不會再把檔名中的 `1800` 當日期。
- 圖片根目錄支援當日與月份歸檔兩種位置，資料夾名稱兼容 `晚報yt縮圖`、`YT縮圖`。

### 3.2 標圖版左邊字

- 引號內的變色規則在左邊字一律忽略，主文字替換 `直標/直標直標`。
- 單一半形數字或單一標點改為全形並保留在主文字層。
- 連續兩個以上的半形數字／符號組合（如 `!!`、`3%`、`3:1`、`-3!`、`300`）改用獨立橫向文字層；主文字層以兩個空格保留位置。
- 純兩位整數使用 kerning 90；其他獨立橫向組合使用 kerning 110。
- 獨立層寬度會比照主文字層最後一個非空白字的寬度，不再固定 250px。
- 獨立層高度以「含兩個空格」和「不含空格」的定位差計算，再乘 95%，避免陰影壓到下方文字。
- 獨立層與主文字層對應位置置中，不移動主文字層；完成後主文字層及所有獨立層一起旋轉 `-4.08°`。
- `直標` 群組最終高度上限 1000px。

### 3.3 標圖版圖片

- 圖片全部以 Embedded Smart Object 匯入 `圖片` 群組。
- 單圖等比縮放至 W=1400，中心 Y=260；套用 Contract 20px 的遮色片與 Feather 20px。
- 恰好兩張圖時，左圖右側套用漸層虛化；右圖置於左圖下方並從拼接位置向左重疊 215px。
- 三張以上時全部以智慧型物件匯入並使用多圖排列；缺少的個別圖片不再阻止其他已找到圖片輸出。

### 3.4 大標文字規則

- 標圖版：大標1只做水平變形至 W=1380px，大標2只做水平變形至 W=1340px，垂直比例固定 100%，不再套用額外最終上限；大標1最後一字 baseline shift `-17.88px`。
- 大標版：大標1 W=1500px；大標2 W=1560px；大標1最後一字 baseline shift `-30px`。
- 大標寬度水平變形在效果字、特殊字級與 Kerning 完成後執行；行首 `「`／行尾 `」` 的位置例外最後套用。
- 行尾已知效果字漏打左括號時會自動修復，例如 `標題 綠色漫畫爆炸)` 解析為大標 `標題` 與效果字 `綠色漫畫爆炸`。
- 書名號 `《》`：`《` 前方有字時前方 Kerning=2300；行首 `《` 在寬度變形後左移 100px；只要 `》` 後方有字，就設定 `》` 後方 Kerning=-200，`》` 前方不修改。
- 只有大標版將 `「`、`」` 字級設為 400pt，並將 `「` baseline shift 設為 +80pt；標圖版不修改兩者字級或 baseline shift。
- 若 `「` 位於行首，取消前方 kerning，改為整個大標圖層向左移 140px。
- 若 `「` 前方有文字，將 `「` 前方 kerning 設為 -300；若對應的 `」` 後方仍有文字，另將 `」` 後方 kerning 設為 -300，`」` 前方不修改。建立多個 kerning range 時需由較大的 `From` 位置往較小位置寫入。
- 若去除尾端空白後 `」` 是最後一字，大標圖層以左側為錨點向右增加 70px 寬度。
- `%` 最終規則改為固定字級：大標版 350pt、標圖版 200pt。多個 `%` 會集中在同一個 `textStyleRange` ActionList 內套用，保留原字色與效果。
- `字小`／`字可以小一點`：大標版大標1=450pt、大標2=441pt；標圖版大標1=250pt、大標2=245pt。

### 3.5 GUI 與工作流程

- 日期欄按 Enter 可切換至該日期的文字來源；瀏覽其他日期資料夾也會同步切換讀圖與輸出日期。
- 雙擊左欄文字檔可用 Windows 預設編輯器開啟。
- 新增右欄 `縮圖設定`：只有選到縮圖時顯示，前四欄依序為版型、主播、隨機配色、右上變色，並包含可選左邊字、兩行大標、圖片指示、實際圖片小圖、變色／效果文字、驗證結果及完整文字檔內容。
- `套用設定` 會立即重新生成；`還原設定` 會還原原始文字解析結果後重新生成。
- 本次工作階段實際隨機選到的配色與右上變色會由 `generation_report` 回傳並保存，重開設定欄不再顯示錯誤組別。
- 左右欄預設固定窄寬，文字或設定內容過長時自動擴寬，避免下方水平捲軸。
- 重新生成時隱藏舊縮圖，只顯示內建的 `cg-indigo-loader.svg`；動畫已納入 EXE 打包資源，不再依賴個人 Documents 路徑，找不到時仍顯示文字後備。
- `待生成／已完成／失敗` 已移到待生成縮圖標題列，並依實際任務狀態更新。

### 3.6 配色來源

- 配色優先讀取 Google Sheet：試算表 ID `1-Rs2Y1wE1X8bfJJuO0bUc2e5BG5dXqvioxo5mCH0YVI`、gid `792890079`。
- 使用 CSV export，五分鐘內不重複下載；快取位於 `%LOCALAPPDATA%\晚報YT縮圖\晚報變色_google.csv`。
- 網路失敗時依序退回快取及專案內 `晚報變色.csv`，並驗證資料格式。
- 修正本地 `晚報變色.csv` 的 B02 第二行底色為 `00731b`。

## 4. 尚未完成／尚未確認

- 最新 `%` 字級（大標版 350pt、標圖版 200pt）尚未由使用者用 Photoshop 確認視覺結果。
- 最新中文引號規則（行首左移 140px、非行首前後 kerning -300、行尾寬度 +70px）尚未完整實機回歸。
- 最新變更後尚未重新跑完整 24 則批次。
- Google Sheet 線上更新、離線快取及本地後備尚未在正式產線環境完整測試。
- GUI 的設定保存、套用／還原、長內容自動寬度與不同 DPI 顯示仍需使用者實際操作確認。
- NAS `10.227.63.105` 在目前環境曾出現 Access denied；可能是帳號憑證問題，不能直接判定路徑不存在。
- `效果字處理.csv` 目前未追蹤，但 `build_exe.bat` 會把它當打包資源；正式打包前必須決定是否納入 Git，不能在本 checkpoint 擅自加入。
- `開啟本地測試.bat` 仍未追蹤，且過去曾發生中文編碼造成雙擊閃退；本次不提交。

## 5. 最近修改檔案與原因

- `parse_thumbnail_txt.py`：圖片指示、`定圖` 候選、多圖容錯、左邊字格式與日期路徑解析。
- `generate_photoshop_script.py`：左邊字數字／符號排版、智慧型圖片匯入、多圖排列、大標尺寸、中文引號與 `%` 格式。
- `gui_main.py`：日期 Enter、雙擊開檔、縮圖設定右欄、CGLoading、欄寬、狀態統計與設定重生流程。
- `worker.py`：回傳並保存每張縮圖的實際生成配置，配合 GUI 顯示與重新生成。
- `test_layout_variants.py`：新增及更新解析、圖片、數字符號、引號、百分比、效果字、配色與 JSX 字串斷言；目前共 45 項測試。
- `build_exe.bat`、`cg-indigo-loader.svg`：將重新生成動畫納入打包資源，避免換電腦後動畫消失。
- `晚報變色.csv`：修正 B02 底色為 `00731b`。
- `AGENTS.md`：同步目前長期架構、限制、排版數值與測試命令。
- `PROJECT_HANDOVER.md`：本次更新至目前進度及 checkpoint 範圍。

## 6. 已嘗試但失敗的方法

### 6.1 中文引號前方 kerning

- 對 range `0→1` 設 kerning：Photoshop 實際改到 `「` 後方。
- 對 range `-1→0` 設 kerning：Photoshop 忽略。
- 在行首插入 U+200B 再設 -300：資料中可看到數值，但畫面位置沒有變化。
- 依 400pt、文件解析度與 transform 計算補償：結果不穩定，已放棄。
- 最終改為明確規則：行首整層左移 140px；只有前方已有文字時才套用前後 kerning -300。

### 6.2 百分比字級

- 依目前 point size 乘 70%：ScriptListener 數值看似正確，但 `%` 的實際可見字形仍遠小於預期。
- 建立複製探針圖層、用 `boundsNoEffects` 量可見高度再反算比例：使用者測試仍無改善，已移除。
- 最終採固定字級：大標版 350pt、標圖版 200pt，仍待視覺確認。

### 6.3 其他

- NAS 路徑回報 Access denied 時，不能靠改資料夾名稱繞過權限；須先確認 Windows/NAS 登入狀態。
- 本地 BAT 曾因編碼把中文命令解讀成亂碼而閃退；不要把該 BAT 當唯一診斷方式。

## 7. 重要架構決策

- Photoshop JSX + 既有 PSD 模板是固定產線，不改用 PIL、ImageMagick 或其他渲染器。
- 大標版使用 `晚報YT縮圖.psd`；標圖版使用 `晚報YT縮圖(標圖版).psd`。
- 標圖版圖片必須使用 Embedded Smart Object，以免縮放後模糊。
- 文字來源通常為 `\\10.227.58.117\新聞txt\MMDD\1800`；輸出依選定日期切換；圖片根目錄為 `\\10.227.63.105\public\__CG-IN`，再依當日或月份歸檔位置搜尋。
- JSX 的文字 style/kerning 必須使用 Photoshop Action Manager；不同範圍應合併及按安全順序寫入，避免後一個 range 覆蓋前一個。
- Google Sheet 是配色主要來源，但必須保留本機快取與 CSV 後備，不能讓網路中斷阻止產線。

## 8. 目前已知問題與風險

- 單元測試只驗證 Python 解析與 JSX 文字，不會執行 Photoshop；ActionDescriptor 在不同 Photoshop 狀態下仍可能有差異。
- 引號、百分比、變色字、效果字同時出現時，Photoshop 的 style range 可能互相覆蓋，需用實際模板做組合測試。
- 多圖排版及缺圖容錯已由程式與測試覆蓋，但三張以上的視覺位置仍可能需要人工確認。
- NAS 存取問題可能來自憑證，不應把 Access denied 當成找不到資料夾。
- 本機有大型 Photoshop ScriptingListener log（先前約 39GB）；不可整檔掃描或加入 Git。
- 工作區有新聞文字、測試資料夾、輸出、log、聊天紀錄及本地 BAT 等未追蹤檔案；不可使用 `git add .`。
- `效果字處理.csv` 是未追蹤但可能必要的打包依賴，下一個 agent 必須先確認其來源、編碼及是否適合版本控制。

## 9. 測試結果

本次 checkpoint 前已執行：

```text
python -W error -m py_compile parse_thumbnail_txt.py generate_photoshop_script.py gui_main.py worker.py
通過

python -m unittest test_layout_variants.py
Ran 45 tests ... OK

git diff --check
無 whitespace error；只有 Git 的 LF/CRLF 與全域設定警告
```

## 10. Git 狀態與 checkpoint 範圍

- 本次部署的基底 HEAD：`ed50b91 feat: harden parsing and quote review workflow`。
- 部署提交包含：
  - `AGENTS.md`
  - `PROJECT_HANDOVER.md`
  - `generate_photoshop_script.py`
  - `gui_main.py`
  - `parse_thumbnail_txt.py`
  - `test_layout_variants.py`
  - `build_exe.bat`
  - `cg-indigo-loader.svg`
- 不提交：PSD、新聞／測試文字、測試輸出、cache、log、`chat.json`、`copilot-context.md`、本地 BAT、未確認來源的 `效果字處理.csv`、憑證或任何秘密。
- 實際部署 commit 編號以 `git log -1 --oneline` 為準。

## 11. Next Step

1. 先以 Photoshop 分別測一則大標版與標圖版含 `%` 的文字，確認 350pt／200pt 視覺大小。
2. 測試三種中文引號：行首 `「`、`「` 前方有文字、行尾為 `」`；確認左移 140px、條件 kerning -300、向右增加 70px。
3. 測試同一行含多個 `%`、引號、變色字與效果字，確認 style range 沒有互相覆蓋。
4. 跑完整 24 則批次，核對 GUI 的待生成／已完成／失敗、實際配色紀錄、套用設定及還原設定。
5. 在正式網路環境測 Google Sheet 更新、五分鐘節流、離線快取與本地 CSV 後備；另確認 NAS 憑證與圖片路徑。
6. 下次正式打包前決定 `效果字處理.csv` 與 `開啟本地測試.bat` 是否納入 Git；目前仍維持未追蹤，不要擅自提交。
7. 持續依使用者的 Photoshop 本地預覽結果微調，再決定下一個正式 EXE 版本。

## 12. 給下一個 Codex agent 的關鍵上下文

- 先讀本文件、`AGENTS.md`、`git status`、`git log -1 --oneline`，不要假設未追蹤檔都可提交。
- 最新 `%` 與 `「」` 規則是使用者在多次視覺測試後指定的明確替代方案；不要恢復 70% 或 U+200B kerning 方法。
- 使用者偏好可重現的本地測試，不希望用控制 Windows GUI 的方式反覆操作 Photoshop；實機視覺驗證應由使用者明確執行或要求。
- 不要把 NAS Access denied 說成路徑不存在，也不要為此隨意改固定網路根目錄。
- 先完成上述實機回歸，再做新的排版調整；若視覺仍不符，保留產生的 JSX 與對應 PSD，針對單一規則診斷。
